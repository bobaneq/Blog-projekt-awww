@model IEnumerable<Komentarz>

@{
    ViewData["Title"] = "Lista komentarzy";
}



<div class="row">
    <div class=col-md-8 offset-md-2>

        <table class="table">
           <thead>

             <tr class="text-center"> 
                <th>@Html.DisplayNameFor(model => model.Wpis) </th>
            </tr>
           
          </thead>
        
          <tbody>
              @foreach (var item in Model)
             {
                <tr>
                    <td class="align-middle"> 
                        @Html.DisplayFor(modelItem => item.Tresc)
                    </td>
                    <td class="align-middle"> 
                        <a class="btn btn-outline-primary"><i class="bi bi-pencil-square">Edycja</i></a> |
                        <a class="btn btn-outline-info"><i class="bi bi-eye">Szczegóły</i></a> |
                        <a class="btn btn-outline-danger"><i class="bi bi-trash">Kasuj</i></a> 
                    </td>





                </tr>
             }

          </tbody>   


        </table>


    </div>

</div>